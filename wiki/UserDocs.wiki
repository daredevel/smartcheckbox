#summary SmartCheckbox usage documentation

== options ==

|| *options* || *description* || *default value* ||
|| attribute || select the attribute used to identify checkboxes to check/uncheck || id ||
|| cascade || if true, the script acts also if the checkbox's state is changed by the script itself || false ||
|| onCheck || || ||
|| onUncheck || || ||